version: 1
applications:
  - appRoot: apps/web
    frontend:
      platform: WEB_COMPUTE
      phases:
        preBuild:
          commands:
            - npm install -g pnpm@9.0.0
            - cd ../.. && pnpm install --frozen-lockfile
            - cd ../.. && pnpm --filter @devflow/core exec prisma generate
        build:
          commands:
            # ✅ Capturamos la ruta de la aplicación activa (apps/web) antes de cualquier CD
            - export REPO_ROOT=$(pwd) && cd ../.. && pnpm --filter web run build
            - export REPO_ROOT=$(pwd) && cp -r $REPO_ROOT/.next/static $REPO_ROOT/.next/standalone/apps/web/.next/static
            - export REPO_ROOT=$(pwd) && ([ -d $REPO_ROOT/public ] && cp -r $REPO_ROOT/public $REPO_ROOT/.next/standalone/apps/web/ || echo "No public dir")
      artifacts:
        baseDirectory: .next/standalone/apps/web
        files:
          - '**/*'
      startCommand: node server.js
      cache:
        paths:
          - ../../node_modules/**/*
          - .next/cache/**/*
