# AWS RDS PostgreSQL Connection
# Replace with your actual credentials: postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=public
DATABASE_URL="postgresql://postgres:password@localhost:5432/promptcraft?schema=public"
